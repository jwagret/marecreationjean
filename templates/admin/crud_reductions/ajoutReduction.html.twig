{% extends 'admin/admin/admin.html.twig' %}

{% block stylesheets %}
  {{ encore_entry_link_tags('datepicker') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('datepicker') }}
{% endblock %}

 {% block admin_section_main_2 %}
     {{ include('includes/retourDashboard.html.twig') }}
     <div class="cadreForm mt-5">
         <p><span>*</span> champs obligatoire</p>
         <h1 class="h3 mb-3 font-weight-normal">{% if nomFormulaire == 'ajouter' %}ajouter une réduction {% else %} modifier une réduction {% endif %}</h1>

         {{ form_start(formReduction, {'attr': {'class': 'containerForm'}}) }}
         <div class="form">
             <div class="row">
                 <div class="col">
                     {{ form_row(formReduction.reduction_reference) }}
                     {{ form_row(formReduction.reduction_designation) }}
                     {{ form_row(formReduction.reduction_pourcentage) }}
                     {{ form_row(formReduction.reduction_montant) }}
                     {{ form_row(formReduction.anneeReductions) }}
                 </div>
             </div>
             <div class="form_btn_align">
{#                 {% if desactiver == false %}#}
                     <button class="btn btn-lg btn-primary mt-3 btn_Form w-100 ms-3" type="submit" value="Valider" name="btn_valider">Valider</button>
                     <button class="btn btn-lg btn-primary mt-3 btn_Form w-100 ms-3" type="submit" value="Annuler" name="btn_annuler">Annuler</button>
{#                 {% endif %}#}
             </div>
         </div>
         {{ form_end(formReduction) }}
     </div>

 {% endblock %}

 {% block admin_section_main_2_2 %}

 {% endblock %}